# Adaptive Strategy Workflow

This portable workflow demonstrates an adaptive MatEnsemble strategy that watches chore completions and returns `ChoreSpec` objects to extend the workflow.

It can run on Frontier, Perlmutter, Pathfinder, Linux containers, or another Flux-capable runtime once launched through the appropriate system environment.
