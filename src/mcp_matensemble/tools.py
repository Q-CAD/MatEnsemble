from .server import mcp

# validate the workflow
# make sure that file paths are accessible, the workflow graph is acyclic, every
# chore has a unique ID, required fields are present for each chore, PYTHON chores
# are importable, and the Chore objects are clean.
